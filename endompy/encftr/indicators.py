"""ENCFT income and labour indicators. Generated from reviewed survey rules."""
from .rules import run_rule

def ing_mensual_ocup_prin_asalariado(tbl):
    """Compute ing_mensual_ocup_prin_asalariado; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_mensual_ocup_prin_asalariado.
    """
    return run_rule(tbl, "ing_mensual_ocup_prin_asalariado")

def ing_mensual_ocup_prin_independiente(tbl):
    """Compute ing_mensual_ocup_prin_independiente; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_mensual_ocup_prin_independiente.
    """
    return run_rule(tbl, "ing_mensual_ocup_prin_independiente")

def ing_mensual_ocup_prin_cuenta_propia(tbl):
    """Compute ing_mensual_ocup_prin_cuenta_propia; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_mensual_ocup_prin_cuenta_propia.
    """
    return run_rule(tbl, "ing_mensual_ocup_prin_cuenta_propia")

def ing_mensual_ocup_prin(tbl):
    """Compute ing_mensual_ocup_prin; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_mensual_ocup_prin.
    """
    return run_rule(tbl, "ing_mensual_ocup_prin")

def ing_comisiones(tbl):
    """Compute ing_comisiones; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_comisiones.
    """
    return run_rule(tbl, "ing_comisiones")

def ing_propinas(tbl):
    """Compute ing_propinas; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_propinas.
    """
    return run_rule(tbl, "ing_propinas")

def ing_horas_extra(tbl):
    """Compute ing_horas_extra; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_horas_extra.
    """
    return run_rule(tbl, "ing_horas_extra")

def ing_vacaciones(tbl):
    """Compute ing_vacaciones; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_vacaciones.
    """
    return run_rule(tbl, "ing_vacaciones")

def ing_dividendos(tbl):
    """Compute ing_dividendos; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_dividendos.
    """
    return run_rule(tbl, "ing_dividendos")

def ing_bonificaciones(tbl):
    """Compute ing_bonificaciones; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_bonificaciones.
    """
    return run_rule(tbl, "ing_bonificaciones")

def ing_regalia_pascual(tbl):
    """Compute ing_regalia_pascual; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_regalia_pascual.
    """
    return run_rule(tbl, "ing_regalia_pascual")

def ing_utilidades_empresariales(tbl):
    """Compute ing_utilidades_empresariales; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_utilidades_empresariales.
    """
    return run_rule(tbl, "ing_utilidades_empresariales")

def ing_beneficios_marginales(tbl):
    """Compute ing_beneficios_marginales; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_beneficios_marginales.
    """
    return run_rule(tbl, "ing_beneficios_marginales")

def ing_otros_ocup_sec_asalariado(tbl):
    """Compute ing_otros_ocup_sec_asalariado; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_otros_ocup_sec_asalariado.
    """
    return run_rule(tbl, "ing_otros_ocup_sec_asalariado")

def ing_beneficios_marginales_anual(tbl):
    """Compute ing_beneficios_marginales_anual; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_beneficios_marginales_anual.
    """
    return run_rule(tbl, "ing_beneficios_marginales_anual")

def ing_especie_alimentos(tbl):
    """Compute ing_especie_alimentos; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_alimentos.
    """
    return run_rule(tbl, "ing_especie_alimentos")

def ing_especie_viviendas(tbl):
    """Compute ing_especie_viviendas; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_viviendas.
    """
    return run_rule(tbl, "ing_especie_viviendas")

def ing_especie_transporte(tbl):
    """Compute ing_especie_transporte; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_transporte.
    """
    return run_rule(tbl, "ing_especie_transporte")

def ing_especie_combustible(tbl):
    """Compute ing_especie_combustible; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_combustible.
    """
    return run_rule(tbl, "ing_especie_combustible")

def ing_especie_celular(tbl):
    """Compute ing_especie_celular; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_celular.
    """
    return run_rule(tbl, "ing_especie_celular")

def ing_especie_otros(tbl):
    """Compute ing_especie_otros; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_otros.
    """
    return run_rule(tbl, "ing_especie_otros")

def ing_especie_cuenta_propia(tbl):
    """Compute ing_especie_cuenta_propia; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_cuenta_propia.
    """
    return run_rule(tbl, "ing_especie_cuenta_propia")

def ing_especie_ocup_sec_asalarariado(tbl):
    """Compute ing_especie_ocup_sec_asalarariado; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_ocup_sec_asalarariado.
    """
    return run_rule(tbl, "ing_especie_ocup_sec_asalarariado")

def ing_especie_ocup_sec_cuenta_propia(tbl):
    """Compute ing_especie_ocup_sec_cuenta_propia; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_ocup_sec_cuenta_propia.
    """
    return run_rule(tbl, "ing_especie_ocup_sec_cuenta_propia")

def ing_mensual_ocup_sec_asalariado(tbl):
    """Compute ing_mensual_ocup_sec_asalariado; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_mensual_ocup_sec_asalariado.
    """
    return run_rule(tbl, "ing_mensual_ocup_sec_asalariado")

def ing_mensual_ocup_sec_independiente(tbl):
    """Compute ing_mensual_ocup_sec_independiente; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_mensual_ocup_sec_independiente.
    """
    return run_rule(tbl, "ing_mensual_ocup_sec_independiente")

def ing_mensual_ocup_sec_cuenta_propia(tbl):
    """Compute ing_mensual_ocup_sec_cuenta_propia; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_mensual_ocup_sec_cuenta_propia.
    """
    return run_rule(tbl, "ing_mensual_ocup_sec_cuenta_propia")

def ing_pension_jubilacion(tbl):
    """Compute ing_pension_jubilacion; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_pension_jubilacion.
    """
    return run_rule(tbl, "ing_pension_jubilacion")

def ing_intereses_dividendos(tbl):
    """Compute ing_intereses_dividendos; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_intereses_dividendos.
    """
    return run_rule(tbl, "ing_intereses_dividendos")

def ing_alquileres_renta(tbl):
    """Compute ing_alquileres_renta; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_alquileres_renta.
    """
    return run_rule(tbl, "ing_alquileres_renta")

def ing_remesas_nacionales(tbl):
    """Compute ing_remesas_nacionales; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_remesas_nacionales.
    """
    return run_rule(tbl, "ing_remesas_nacionales")

def ing_pension_jubilacion_anual(tbl):
    """Compute ing_pension_jubilacion_anual; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_pension_jubilacion_anual.
    """
    return run_rule(tbl, "ing_pension_jubilacion_anual")

def ing_intereses_dividendos_anual(tbl):
    """Compute ing_intereses_dividendos_anual; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_intereses_dividendos_anual.
    """
    return run_rule(tbl, "ing_intereses_dividendos_anual")

def ing_alquileres_renta_anual(tbl):
    """Compute ing_alquileres_renta_anual; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_alquileres_renta_anual.
    """
    return run_rule(tbl, "ing_alquileres_renta_anual")

def ing_remesas_nacionales_anual(tbl):
    """Compute ing_remesas_nacionales_anual; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_remesas_nacionales_anual.
    """
    return run_rule(tbl, "ing_remesas_nacionales_anual")

def ing_especie_ayuda_ong(tbl):
    """Compute ing_especie_ayuda_ong; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_ayuda_ong.
    """
    return run_rule(tbl, "ing_especie_ayuda_ong")

def ing_especie_ayuda_ong_anual(tbl):
    """Compute ing_especie_ayuda_ong_anual; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_especie_ayuda_ong_anual.
    """
    return run_rule(tbl, "ing_especie_ayuda_ong_anual")

def ing_autoconsumo_autosuministro(tbl):
    """Compute ing_autoconsumo_autosuministro; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_autoconsumo_autosuministro.
    """
    return run_rule(tbl, "ing_autoconsumo_autosuministro")

def ing_pension_ext(tbl):
    """Compute ing_pension_ext; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_pension_ext.
    """
    return run_rule(tbl, "ing_pension_ext")

def ing_intereses_dividendos_ext(tbl):
    """Compute ing_intereses_dividendos_ext; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_intereses_dividendos_ext.
    """
    return run_rule(tbl, "ing_intereses_dividendos_ext")

def ing_alquileres_renta_ext(tbl):
    """Compute ing_alquileres_renta_ext; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_alquileres_renta_ext.
    """
    return run_rule(tbl, "ing_alquileres_renta_ext")

def ing_regalos_ext(tbl):
    """Compute ing_regalos_ext; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_regalos_ext.
    """
    return run_rule(tbl, "ing_regalos_ext")

def ing_alquiler_imputado(tbl, deflactar=True):
    """Compute ing_alquiler_imputado; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_alquiler_imputado.
    """
    return run_rule(tbl, "ing_alquiler_imputado", deflactar=deflactar)

def ing_laboral_monetario(tbl, deflactar=True, keep=False, reuse=False):
    """Compute ing_laboral_monetario; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_laboral_monetario.
    """
    return run_rule(tbl, "ing_laboral_monetario", deflactar=deflactar, keep=keep, reuse=reuse)

def ing_no_monetario_laboral(tbl, deflactar=True, keep=False, reuse=False):
    """Compute ing_no_monetario_laboral; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_no_monetario_laboral.
    """
    return run_rule(tbl, "ing_no_monetario_laboral", deflactar=deflactar, keep=keep, reuse=reuse)

def ing_monetario_no_laboral(tbl, deflactar=True, keep=False, reuse=False):
    """Compute ing_monetario_no_laboral; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_monetario_no_laboral.
    """
    return run_rule(tbl, "ing_monetario_no_laboral", deflactar=deflactar, keep=keep, reuse=reuse)

def ing_no_monetario_no_laboral(tbl, deflactar=True, keep=False, reuse=False):
    """Compute ing_no_monetario_no_laboral; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_no_monetario_no_laboral.
    """
    return run_rule(tbl, "ing_no_monetario_no_laboral", deflactar=deflactar, keep=keep, reuse=reuse)

def ing_transferencias_sociales(tbl, deflactar=True):
    """Compute ing_transferencias_sociales; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_transferencias_sociales.
    """
    return run_rule(tbl, "ing_transferencias_sociales", deflactar=deflactar)

def ing_monetario_ext(tbl, deflactar=True, keep=False, reuse=False):
    """Compute ing_monetario_ext; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ing_monetario_ext.
    """
    return run_rule(tbl, "ing_monetario_ext", deflactar=deflactar, keep=keep, reuse=reuse)

def perceptores_ingresos(tbl, min_edad=15):
    """Compute perceptores_ingresos; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_perceptores_ingresos.
    """
    return run_rule(tbl, "perceptores_ingresos", min_edad=min_edad)

def ingreso_laboral_total(tbl):
    """Compute ingreso_laboral_total; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_ingreso_laboral_total.
    """
    return run_rule(tbl, "ingreso_laboral_total")

def horas_semana(tbl):
    """Compute horas_semana; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_horas_semana.
    """
    return run_rule(tbl, "horas_semana")

def fuerza_trabajo_potencial(tbl):
    """Compute fuerza_trabajo_potencial; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_fuerza_trabajo_potencial.
    """
    return run_rule(tbl, "fuerza_trabajo_potencial")

def tiempo_total_empleo_dias(tbl):
    """Compute tiempo_total_empleo_dias; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_tiempo_total_empleo_dias.
    """
    return run_rule(tbl, "tiempo_total_empleo_dias")

def tiempo_total_empleo_meses(tbl):
    """Compute tiempo_total_empleo_meses; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_tiempo_total_empleo_meses.
    """
    return run_rule(tbl, "tiempo_total_empleo_meses")

def tiempo_total_empleo_anos(tbl):
    """Compute tiempo_total_empleo_anos; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_tiempo_total_empleo_anos.
    """
    return run_rule(tbl, "tiempo_total_empleo_anos")

def grupo_rama_pib(tbl):
    """Compute grupo_rama_pib; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_grupo_rama_pib.
    """
    return run_rule(tbl, "grupo_rama_pib")

def regiones_desarrollo(tbl):
    """Compute regiones_desarrollo; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_regiones_desarrollo.
    """
    return run_rule(tbl, "regiones_desarrollo")

def zona_desarrollo_fronterizo(tbl):
    """Compute zona_desarrollo_fronterizo; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_zona_desarrollo_fronterizo.
    """
    return run_rule(tbl, "zona_desarrollo_fronterizo")

def recode_material_pared_exterior(tbl):
    """Compute recode_material_pared_exterior; see the bilingual reference for inputs, units and missing-value rules.

    Returns a copy of tbl with the indicator columns. Input rows are preserved.
    Corresponding R function: encftr::ftc_recode_material_pared_exterior.
    """
    return run_rule(tbl, "recode_material_pared_exterior")
